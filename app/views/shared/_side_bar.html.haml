.side
  .side__header
    .side__header__box
      %h3.side__header__box__name
        = current_user.name
      %ul.side__header__box__lists
        %li.list
          = link_to new_group_path do
            = fa_icon 'pencil-square-o', class: 'icon'
        %li.list
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog', class: 'icon'
  .side__groups
    - current_user.groups.each do |group|
      .side__groups__group
        = link_to '#' do
          .side__groups__group__name
            = group.name
          .side__groups__group__info
            メッセージはまだありません。